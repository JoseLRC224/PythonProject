# Mi Proyecto Python 🐍

Este es mi primer repositorio configurado desde **WSL2** y listo para ser portado a **Fedora Linux**.

## 🚀 Setup del Proyecto
Este proyecto utiliza un entorno virtual para mantener las dependencias aisladas.

### Requisitos
- Python 3.x
- Git

### Instalación
1. Clonar el repositorio.
2. Crear el entorno virtual: `python3 -m venv .venv`
3. Activar el entorno: `source .venv/bin/activate`
4. Instalar dependencias: `pip install -r requirements.txt` (próximamente)

---
*Configurado con la ayuda de Gemini para el setup de Bit.*
